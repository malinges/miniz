if(IDF_VERSION_MAJOR GREATER_EQUAL 4)
    idf_component_register(SRC_DIRS src INCLUDE_DIRS .)
else()
    set(COMPONENT_SRCDIRS .)
    set(COMPONENT_ADD_INCLUDEDIRS .)
    register_component()
endif()
